const values = 'renda = 3000.0\ndespesa_1 = 1200.0\ndespesa_2 = 450.0\ndespesa_3 = 300.0';
const sum = '\ntotal_despesas = despesa_1 + despesa_2 + despesa_3';
const balance = '\nsaldo = renda - total_despesas';
const inputs = 'texto = input("Qual é sua renda? ")\nrenda = float(texto)\ntexto = input("Qual é a primeira despesa? ")\ndespesa_1 = float(texto)\ntexto = input("Qual é a segunda despesa? ")\ndespesa_2 = float(texto)\ntexto = input("Qual é a terceira despesa? ")\ndespesa_3 = float(texto)';
export const calculatorGuide = {
  valores: { why: 'Primeiro vamos guardar números que já conhecemos. Ainda não precisamos perguntar nada.', actions: ['Escreva renda = 3000.0. O ponto marca as casas decimais.', 'Crie uma linha para cada despesa, como no exemplo.', 'No final, print(renda) mostra só a renda. Execute: deve aparecer 3000.0.'], example: values + '\nprint(renda)', explain: 'Cada = guarda o valor que está à direita no nome à esquerda. Não coloque input aqui ainda.', tryIt: 'Depois de conferir, troque renda para 4000.0 e preveja a saída. Volte para 3000.0 antes de avançar.' },
  total: { why: 'Agora sabemos os valores. Queremos descobrir quanto foi gasto ao todo.', actions: ['Mantenha as quatro variáveis do passo anterior.', 'Acrescente total_despesas = despesa_1 + despesa_2 + despesa_3.', 'Troque print(renda) por print(total_despesas).'], example: values + sum + '\nprint(total_despesas)', explain: '1200 + 450 + 300 = 1950. O nome total_despesas guarda essa soma. Aqui vamos mostrar somente essa soma.', tryIt: 'Se a terceira despesa fosse 400, qual seria o total? Confira e depois restaure 300.' },
  saldo: { why: 'Saldo é o que sobra depois de pagar as despesas.', actions: ['Mantenha os valores e a soma.', 'Acrescente saldo = renda - total_despesas.', 'Use print(f"{saldo:.2f}") para mostrar duas casas. Copie esse formato com calma: o f vem antes das aspas, e saldo fica entre chaves.'], example: values + sum + balance + '\nprint(f"{saldo:.2f}")', explain: '3000 - 1950 = 1050. A parte :.2f só faz aparecer 1050.00; não muda o valor da conta.', tryIt: 'Compare print(saldo) e print(f"{saldo:.2f}") em duas execuções. O valor da conta mudou ou apenas a apresentação?' },
  pergunta: { why: 'Vamos treinar uma pergunta sozinha antes de mudar o orçamento inteiro.', actions: ['Veja o exemplo de duas linhas. A primeira pergunta e guarda a resposta como texto.', 'Execute e digite 1200 no campo que aparecer abaixo do editor.', 'A segunda linha mostra o que você respondeu.'], example: 'texto = input("Quanto é sua despesa? ")\nprint(texto)', explain: 'input("Quanto é sua despesa? ") faz a pergunta. texto recebe a resposta. input() sem mensagem também espera uma resposta, mas não tem uma pergunta escrita.', tryIt: 'Mude apenas a frase entre aspas. A pergunta deve mudar, sem precisar alterar print.' },
  conversao: { why: 'input entrega texto. Para fazer contas com dinheiro, vamos transformar esse texto em número.', actions: ['Leia a resposta em texto.', 'Na linha seguinte, escreva despesa_1 = float(texto).', 'Mostre despesa_1 e responda 1200. Deve aparecer 1200.0.'], example: 'texto = input("Quanto é sua despesa? ")\ndespesa_1 = float(texto)\nprint(despesa_1)', explain: 'Não escreva despesa_1 = input() = float(despesa_1). São duas ações em duas linhas: ler a resposta, depois converter. float aceita 1200.50 com ponto; 1200,50 com vírgula causa erro.', tryIt: 'Experimente responder 1200.50 e observe o número. Depois use 1200 para conferir este passo.' },
  entrada: { why: 'Agora vamos repetir a dupla “perguntar e converter” para cada um dos quatro valores.', actions: ['Para cada pergunta, guarde a resposta em texto e converta na linha seguinte.', 'Mantenha a soma e o cálculo de saldo depois das quatro perguntas.', 'Responda na ordem: 3000, 1200, 450 e 300. Ao final, confira 1050.00.'], example: inputs + sum + balance + '\nprint(f"{saldo:.2f}")', explain: 'Reutilizar texto é permitido: a cada pergunta ele recebe a nova resposta. renda e as despesas já guardaram seus números nas linhas de conversão.', tryIt: 'Teste também 4000, 1000, 500 e 500: deve sobrar 2000.00. O programa deve calcular com as respostas, sem imprimir um saldo fixo.' },
  relatorio: { why: 'Vamos deixar claro o que cada número da resposta significa.', actions: ['Mantenha as perguntas e as contas.', 'Troque o print final pelas três linhas do exemplo: renda, despesas e saldo.', 'Teste os três conjuntos abaixo. Só marque o requisito de testes depois de conferir cada resultado.'], example: inputs + sum + balance + '\nprint(f"Renda: {renda:.2f}")\nprint(f"Despesas: {total_despesas:.2f}")\nprint(f"Saldo: {saldo:.2f}")', explain: 'O texto Renda: aparece como foi escrito. O trecho entre chaves é substituído pelo valor da variável.', tryIt: 'Teste A: 3000 / 1200 / 450 / 300 → saldo 1050.00. Teste B: 4000 / 1000 / 500 / 500 → saldo 2000.00. Teste C: 1000 / 600 / 300 / 200 → saldo -100.00.' }
};
